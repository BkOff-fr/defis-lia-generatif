# C33.3 — 4 sections 3D animées — Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Livrer les 4 sections animées du landing (Méthodologies Monte-Carlo, Vendors disclosure, Territoire FR, Entreprises), en remplacement des placeholders 5/6/7/8 actuels.

**Architecture & approche par section :**
- **Monte-Carlo (§5)** : Three.js `InstancedMesh` 10⁴ sphères, gaussienne autour d'un axe X, convergence animée au scroll, toggle AFNOR/Sobr.ia vs EcoLogits qui re-tirage les paramètres et lerp les positions.
- **Vendors disclosure (§6)** : Astro/Svelte pur, table animée révélée séquentiellement via IntersectionObserver, chiffres clés en grand (1.14 g, 0.03 g, 11 390 t).
- **Territoire FR (§7)** : SVG France tilté CSS 3D (`perspective + rotateX`), datacenters FR plotted en dots lime pulsants, tooltips native title — pas de GeoJSON, pas de Three.js (cf décision arbitrée).
- **Entreprises (§8)** : 2 cards self-hosted vs cloud bientôt + SVG inline schéma (icône serveur Lucide + 4 avatars employés reliés), animation au scroll.

**Tech additions :**
- Aucune nouvelle dépendance. Réutilise Three.js déjà installé (lazy-loaded depuis HeroGlobe), `@lucide/svelte` pour icônes, IntersectionObserver pour scroll triggers.

**Source des chiffres vendors** (extraits de `docs/sources/AUDIT-2026-Q3.md`) :
- Mistral Large 2 (FR) : 1.14 gCO₂eq / 400 tokens — ✅ Prompt-level + Training disclosure (partenariat ADEME)
- Google Gemini Apps (median) : 0.03 gCO₂eq + 0.24 Wh + 0.26 mL — ✅ Prompt-level + ⚠️ Training partial
- Meta Llama 3.1 : 11 390 tCO₂eq location-based (training) — ❌ Prompt-level + ✅ Training
- Anthropic Claude : pas de disclosure officielle — ❌/❌
- OpenAI GPT-4o : pas de disclosure officielle — ❌/❌

**Source datacenters FR** : on filtre `site/src/data/datacenters.json` (déjà en place depuis C33.2) sur `country === 'FR'`.

---

## File Structure

```
site/src/
├── components/
│   ├── three/
│   │   └── MonteCarloViz.svelte    # NEW — Three.js Monte-Carlo
│   └── sections/
│       ├── Methodologies.astro     # NEW — section 5 wrap MonteCarloViz + intro + toggle
│       ├── VendorDisclosure.astro  # NEW — section 6
│       ├── TerritoireFR.astro      # NEW — section 7 (SVG France tilté)
│       └── Entreprises.astro       # NEW — section 8 (2 cards + SVG schéma)
└── pages/
    └── index.astro                 # MODIFY — remplace les 4 placeholders 5/6/7/8
```

---

## Task 1 — Section Méthodologies (Monte-Carlo Three.js)

**Files:**
- Create: `site/src/components/three/MonteCarloViz.svelte`
- Create: `site/src/components/sections/Methodologies.astro`

- [ ] **Step 1.1 — Write `MonteCarloViz.svelte`**

Implémentation Three.js InstancedMesh 10000 sphères. Distribution gaussienne calculée via Box-Muller. Au scroll-entry (IntersectionObserver `threshold 0.3`), les points dansent ~1s puis convergent vers leur position gaussienne. Toggle bouton AFNOR/EcoLogits → recalcule positions cibles avec params différents, lerp vers ces positions sur ~1.2 s. P5/P95 indiqués par 2 plans verticaux ambre semi-transparents.

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { hasWebGL, prefersReducedMotion } from '$lib/three-utils';

  type Method = 'afnor' | 'ecologits';
  let method: Method = $state('afnor');
  let container: HTMLDivElement | null = $state(null);
  let disabled = $state(false);
  let stats = $state({ p5: 0, p50: 0, p95: 0 });

  // Paramètres illustratifs (ordre de grandeur cohérent avec Mistral Large 2 ~1.14 g/prompt).
  const PARAMS: Record<Method, { mean: number; sigma: number; label: string }> = {
    afnor: { mean: 1.4, sigma: 0.45, label: 'AFNOR SPEC 2314 / Sobr.ia' },
    ecologits: { mean: 1.15, sigma: 0.3, label: 'EcoLogits 2024' },
  };

  // Box-Muller : tirage gaussien standard.
  function gaussRand(): number {
    const u = Math.random() || 1e-9;
    const v = Math.random() || 1e-9;
    return Math.sqrt(-2 * Math.log(u)) * Math.cos(2 * Math.PI * v);
  }

  function percentiles(values: number[]): { p5: number; p50: number; p95: number } {
    const sorted = [...values].sort((a, b) => a - b);
    const at = (q: number) => sorted[Math.floor(q * (sorted.length - 1))];
    return { p5: at(0.05), p50: at(0.5), p95: at(0.95) };
  }

  let stopFn: (() => void) | null = null;

  onMount(() => {
    if (!container) return;
    if (!hasWebGL() || prefersReducedMotion()) {
      disabled = true;
      return;
    }
    let canceled = false;

    (async () => {
      const THREE = await import('three');
      if (canceled || !container) return;

      const N = 10000;
      const W = container.clientWidth;
      const H = container.clientHeight;

      const scene = new THREE.Scene();
      const camera = new THREE.PerspectiveCamera(40, W / H, 0.1, 100);
      camera.position.set(0, 0.6, 6);
      camera.lookAt(0, 0, 0);

      const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
      renderer.setSize(W, H);
      renderer.setClearColor(0x000000, 0);
      container.appendChild(renderer.domElement);

      const geom = new THREE.SphereGeometry(0.015, 4, 4);
      const mat = new THREE.MeshBasicMaterial({
        color: 0xc5f04a,
        transparent: true,
        opacity: 0.55,
      });
      const mesh = new THREE.InstancedMesh(geom, mat, N);
      mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
      scene.add(mesh);

      // Axe + bandes P5/P95
      const axisGeom = new THREE.BoxGeometry(5, 0.005, 0.005);
      const axisMat = new THREE.MeshBasicMaterial({ color: 0x46443f });
      const axis = new THREE.Mesh(axisGeom, axisMat);
      axis.position.y = -1;
      scene.add(axis);

      // 2 plans P5 / P95 (ambre semi-transparent)
      const bandMat = new THREE.MeshBasicMaterial({
        color: 0xf5b769,
        transparent: true,
        opacity: 0.18,
        side: THREE.DoubleSide,
      });
      const bandGeom = new THREE.PlaneGeometry(0.02, 2.4);
      const bandP5 = new THREE.Mesh(bandGeom, bandMat);
      const bandP95 = new THREE.Mesh(bandGeom, bandMat);
      scene.add(bandP5, bandP95);

      // Generate target positions for a given method
      const dummy = new THREE.Object3D();
      const positions = new Float32Array(N * 3);
      const targets = new Float32Array(N * 3);
      const samples: number[] = new Array(N);

      function regenerateTargets(m: Method) {
        const { mean, sigma } = PARAMS[m];
        for (let i = 0; i < N; i++) {
          const sample = mean + sigma * gaussRand();
          samples[i] = sample;
          // map sample → x in scene (mean→0, ±3σ→±2)
          const x = ((sample - mean) / (3 * sigma)) * 2;
          const angle = Math.random() * Math.PI * 2;
          const r = Math.random() * 0.15 + 0.02;
          targets[i * 3] = x;
          targets[i * 3 + 1] = Math.sin(angle) * r - 0.5;
          targets[i * 3 + 2] = Math.cos(angle) * r;
        }
        const p = percentiles(samples);
        stats = p;
        // Place bands at P5 / P95 in scene units
        const toX = (s: number) => ((s - mean) / (3 * sigma)) * 2;
        bandP5.position.x = toX(p.p5);
        bandP95.position.x = toX(p.p95);
      }

      // Initialize at scrambled positions (dance phase)
      for (let i = 0; i < N; i++) {
        positions[i * 3] = (Math.random() - 0.5) * 5;
        positions[i * 3 + 1] = (Math.random() - 0.5) * 2.5 - 0.5;
        positions[i * 3 + 2] = (Math.random() - 0.5) * 2;
      }
      regenerateTargets(method);

      // Triggered convergence on scroll-entry
      let converging = false;
      const io = new IntersectionObserver(
        (entries) => {
          for (const e of entries) {
            if (e.isIntersecting) {
              converging = true;
              io.disconnect();
              break;
            }
          }
        },
        { threshold: 0.3 },
      );
      io.observe(container);

      // Re-target when method changes
      const onMethodChange = () => regenerateTargets(method);
      $effect.root(() => {
        $effect(() => {
          // dependency tracking on `method`
          onMethodChange();
        });
      });

      const ro = new ResizeObserver(() => {
        if (!container) return;
        const w = container.clientWidth;
        const h = container.clientHeight;
        camera.aspect = w / h;
        camera.updateProjectionMatrix();
        renderer.setSize(w, h);
      });
      ro.observe(container);

      let raf = 0;
      let last = performance.now();
      const animate = (now: number) => {
        const dt = Math.min((now - last) / 1000, 0.05);
        last = now;

        const lerp = converging ? Math.min(dt * 1.6, 1) : Math.min(dt * 0.4, 1);
        for (let i = 0; i < N; i++) {
          const ix = i * 3;
          positions[ix] += (targets[ix] - positions[ix]) * lerp;
          positions[ix + 1] += (targets[ix + 1] - positions[ix + 1]) * lerp;
          positions[ix + 2] += (targets[ix + 2] - positions[ix + 2]) * lerp;
          dummy.position.set(positions[ix], positions[ix + 1], positions[ix + 2]);
          dummy.updateMatrix();
          mesh.setMatrixAt(i, dummy.matrix);
        }
        mesh.instanceMatrix.needsUpdate = true;
        scene.rotation.y = Math.sin(now * 0.00015) * 0.08;
        renderer.render(scene, camera);
        raf = requestAnimationFrame(animate);
      };
      raf = requestAnimationFrame(animate);

      stopFn = () => {
        cancelAnimationFrame(raf);
        ro.disconnect();
        io.disconnect();
        renderer.dispose();
        geom.dispose();
        mat.dispose();
        axisGeom.dispose();
        axisMat.dispose();
        bandGeom.dispose();
        bandMat.dispose();
        if (container && renderer.domElement.parentNode === container) {
          container.removeChild(renderer.domElement);
        }
      };
    })();

    return () => {
      canceled = true;
      stopFn?.();
    };
  });
</script>

<div class="grid gap-6 lg:grid-cols-[1fr_320px] items-center">
  <div
    bind:this={container}
    class="w-full h-[420px] rounded-lg border border-ivory-4/30 bg-ink-2/40"
    aria-hidden="true"
  ></div>

  <aside class="space-y-4">
    <div class="flex gap-2">
      {#each ['afnor', 'ecologits'] as m (m)}
        <button
          type="button"
          onclick={() => (method = m as Method)}
          class:list={[
            'rounded-md px-3 py-1.5 text-xs font-sans border transition-colors',
            method === m
              ? 'bg-lime-signature text-ink border-lime-signature'
              : 'text-ivory-2 border-ivory-4/40 hover:border-lime-signature/40',
          ]}
        >
          {m === 'afnor' ? 'AFNOR / Sobr.ia' : 'EcoLogits'}
        </button>
      {/each}
    </div>

    {#if !disabled}
      <dl class="grid grid-cols-3 gap-2 text-center">
        <div class="rounded border border-amber/30 bg-amber/5 p-3">
          <dt class="text-[10px] uppercase text-ivory-3 tracking-wider">P5</dt>
          <dd class="mt-1 font-mono text-lg text-amber">{stats.p5.toFixed(2)}</dd>
        </div>
        <div class="rounded border border-lime-signature/40 bg-lime-signature/10 p-3">
          <dt class="text-[10px] uppercase text-ivory-3 tracking-wider">P50</dt>
          <dd class="mt-1 font-mono text-lg text-lime-signature">{stats.p50.toFixed(2)}</dd>
        </div>
        <div class="rounded border border-amber/30 bg-amber/5 p-3">
          <dt class="text-[10px] uppercase text-ivory-3 tracking-wider">P95</dt>
          <dd class="mt-1 font-mono text-lg text-amber">{stats.p95.toFixed(2)}</dd>
        </div>
      </dl>
      <p class="text-xs text-ivory-3">
        gCO₂eq / prompt · {PARAMS[method].label}
      </p>
    {:else}
      <p class="text-xs text-ivory-3">
        Visualisation 3D désactivée (WebGL ou prefers-reduced-motion).
      </p>
    {/if}
  </aside>
</div>
```

- [ ] **Step 1.2 — Write `Methodologies.astro`**

```astro
---
import MonteCarloViz from '$components/three/MonteCarloViz.svelte';
---

<section id="methodologies" class="border-b border-ivory-4/20">
  <div class="max-w-6xl mx-auto px-6 py-24">
    <p class="text-xs uppercase tracking-widest text-ivory-3">5 — Méthodologies</p>
    <h2 class="mt-2 text-4xl md:text-5xl font-display text-ivory">
      Deux méthodes, <span class="italic text-lime-signature">une seule vérité</span>.
    </h2>
    <p class="mt-6 text-ivory-2 max-w-2xl">
      Sobr.ia ne choisit pas pour vous : on tire 10 000 estimations selon AFNOR SPEC 2314
      ou EcoLogits, on affiche l'incertitude (P5–P95) et vous voyez l'ordre de grandeur réel.
    </p>

    <div class="mt-12">
      <MonteCarloViz client:visible />
    </div>

    <p class="mt-6 text-xs text-ivory-3 max-w-2xl">
      Distribution illustrative (ordres de grandeur cohérents avec Mistral Large 2 ~1.14 g
      / 400 tokens). Les paramètres réels sont calculés à partir du référentiel modèles
      multi-méthodologie de Sobr.ia (cf.
      <a class="underline underline-offset-4" href="/adrs/ADR-0012-multi-methodology-engine/"
        >ADR-0012</a
      >).
    </p>
  </div>
</section>
```

---

## Task 2 — Section Vendors disclosure

**Files:**
- Create: `site/src/components/sections/VendorDisclosure.astro`

- [ ] **Step 2.1 — Write `VendorDisclosure.astro`**

```astro
---
type Tone = 'lime' | 'amber' | 'coral';
interface Vendor {
  name: string;
  flag?: string;
  prompt: { label: string; tone: Tone };
  training: { label: string; tone: Tone };
  metric?: string;
  metricSub?: string;
  source: string;
}

const vendors: Vendor[] = [
  {
    name: 'Mistral × ADEME',
    flag: '🇫🇷',
    prompt: { label: '✓ Oui (2025)', tone: 'lime' },
    training: { label: '✓ Oui — ACV complète', tone: 'lime' },
    metric: '1,14 g',
    metricSub: 'CO₂eq / 400 tokens',
    source: 'Mistral AI · partenariat ADEME',
  },
  {
    name: 'Google Gemini',
    prompt: { label: '✓ Oui (2025)', tone: 'lime' },
    training: { label: '~ Partiel', tone: 'amber' },
    metric: '0,03 g',
    metricSub: 'CO₂eq / prompt médian',
    source: 'Google 2025 disclosure',
  },
  {
    name: 'Meta Llama 3.x',
    prompt: { label: '✕ Non', tone: 'coral' },
    training: { label: '✓ Oui — model cards', tone: 'lime' },
    metric: '11 390 t',
    metricSub: 'CO₂eq training (location-based)',
    source: 'Meta Llama 3.1 model card',
  },
  {
    name: 'Anthropic Claude',
    prompt: { label: '✕ Non', tone: 'coral' },
    training: { label: '✕ Non', tone: 'coral' },
    source: 'Estimations tierces uniquement',
  },
  {
    name: 'OpenAI GPT-4o',
    prompt: { label: '✕ Non', tone: 'coral' },
    training: { label: '✕ Non', tone: 'coral' },
    source: 'Estimations tierces uniquement',
  },
];

const toneClasses: Record<Tone, string> = {
  lime: 'text-lime-signature bg-lime-signature/10 border-lime-signature/30',
  amber: 'text-amber bg-amber/10 border-amber/30',
  coral: 'text-coral bg-coral/10 border-coral/30',
};
---

<section id="vendors" class="border-b border-ivory-4/20">
  <div class="max-w-6xl mx-auto px-6 py-24">
    <p class="text-xs uppercase tracking-widest text-ivory-3">6 — Vendors disclosure</p>
    <h2 class="mt-2 text-4xl md:text-5xl font-display text-ivory">
      Qui dit la vérité sur <span class="italic text-lime-signature">son empreinte</span> ?
    </h2>
    <p class="mt-6 text-ivory-2 max-w-2xl">
      Sobr.ia agrège les chiffres publiés <em>officiellement</em> par les fabricants.
      Quand un vendeur ne publie pas, on le dit — et on utilise les estimations tierces
      les plus solides disponibles.
    </p>

    <ol class="mt-12 space-y-3" data-vendor-list>
      {
        vendors.map((v) => (
          <li
            class="vendor-row rounded-lg border border-ivory-4/30 bg-ink-2 p-5 grid items-center gap-4 sm:grid-cols-[1.4fr_1fr_1fr_1.2fr]"
            data-vendor-row
          >
            <div>
              <p class="font-sans text-lg text-ivory">
                {v.flag && <span class="mr-1">{v.flag}</span>}
                {v.name}
              </p>
              <p class="mt-1 text-xs text-ivory-3">{v.source}</p>
            </div>
            <div>
              <p class="text-[10px] uppercase tracking-wider text-ivory-3">
                Prompt-level
              </p>
              <span
                class={`mt-1 inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs ${toneClasses[v.prompt.tone]}`}
              >
                {v.prompt.label}
              </span>
            </div>
            <div>
              <p class="text-[10px] uppercase tracking-wider text-ivory-3">Training</p>
              <span
                class={`mt-1 inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs ${toneClasses[v.training.tone]}`}
              >
                {v.training.label}
              </span>
            </div>
            {v.metric ? (
              <div class="text-right">
                <p class="font-display text-3xl text-lime-signature leading-none">
                  {v.metric}
                </p>
                <p class="mt-1 text-[10px] uppercase tracking-wider text-ivory-3">
                  {v.metricSub}
                </p>
              </div>
            ) : (
              <div class="text-right text-xs text-ivory-3 italic">
                Pas de chiffre officiel disponible
              </div>
            )}
          </li>
        ))
      }
    </ol>

    <p class="mt-6 text-xs text-ivory-3 max-w-2xl">
      Sources complètes :
      <a class="underline underline-offset-4" href="/docs/sources/AUDIT-2026-Q3/"
        >catalogue audit 2026-Q3</a
      >
      · 19 datasets référencés.
    </p>
  </div>
</section>

<style>
  .vendor-row {
    opacity: 0;
    transform: translateY(8px);
    transition:
      opacity 450ms ease-out,
      transform 450ms ease-out;
  }
  .vendor-row[data-revealed='true'] {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  const list = document.querySelector('[data-vendor-list]');
  if (list) {
    const rows = list.querySelectorAll<HTMLElement>('[data-vendor-row]');
    const io = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            rows.forEach((el, i) =>
              setTimeout(() => el.setAttribute('data-revealed', 'true'), i * 110),
            );
            io.disconnect();
            break;
          }
        }
      },
      { threshold: 0.15 },
    );
    io.observe(list);
  }
</script>
```

---

## Task 3 — Section Territoire FR (SVG tilté 2.5D)

**Files:**
- Create: `site/src/components/sections/TerritoireFR.astro`

- [ ] **Step 3.1 — Write `TerritoireFR.astro`**

SVG path simplifié contour France (Albers approx., ~50 points). Datacenters FR plotted en lat/lon → SVG coords via projection grossière. CSS 3D tilt sur le container.

```astro
---
import datacenters from '../../data/datacenters.json';

// Filtre FR. Note : datacenters.json contient ~5 entrées en FR.
const frDcs = datacenters.filter((d) => d.country === 'FR');

// Projection grossière FR → viewBox 600×600. France ≈ lat [42.3, 51.1], lon [-5, 8.3].
const LAT_MIN = 42.3;
const LAT_MAX = 51.5;
const LON_MIN = -5.5;
const LON_MAX = 9.5;

function project(lat: number, lon: number): { x: number; y: number } {
  const x = ((lon - LON_MIN) / (LON_MAX - LON_MIN)) * 600;
  const y = ((LAT_MAX - lat) / (LAT_MAX - LAT_MIN)) * 600;
  return { x, y };
}

// Contour France hexagonal très stylisé — pas de précision géographique exigée
// (le rendu 3D tilté lit comme une silhouette). On dessine les frontières dans
// l'espace lat/lon puis on projette.
const outlineLonLat: [number, number][] = [
  [2.0, 51.1], // Dunkerque
  [4.85, 50.85], // Mons
  [6.15, 49.6], // Luxembourg
  [8.2, 48.95], // Strasbourg
  [7.55, 47.55], // Bâle
  [6.9, 45.95], // Mont Blanc
  [7.45, 43.75], // Menton
  [3.0, 42.5], // Pyrénées Est
  [-1.4, 43.05], // Hendaye
  [-1.8, 46.45], // Olonne
  [-4.8, 48.4], // Pointe Bretagne
  [-1.55, 49.7], // Cherbourg
  [1.6, 50.95], // Calais
  [2.0, 51.1],
];
const outlinePoints = outlineLonLat
  .map(([lon, lat]) => {
    const p = project(lat, lon);
    return `${p.x.toFixed(1)},${p.y.toFixed(1)}`;
  })
  .join(' ');
---

<section id="territoire-fr" class="border-b border-ivory-4/20">
  <div class="max-w-6xl mx-auto px-6 py-24">
    <p class="text-xs uppercase tracking-widest text-ivory-3">7 — Territoire français</p>
    <h2 class="mt-2 text-4xl md:text-5xl font-display text-ivory">
      L'IA générative <span class="italic text-lime-signature">a un territoire</span>.
    </h2>
    <p class="mt-6 text-ivory-2 max-w-2xl">
      Sobr.ia croise les datacenters réels (lat/lon, PUE, intensité carbone par maille)
      avec ComparIA et le dataset RTE IRIS — pour la première fois, l'empreinte est
      géographiquement située à l'échelle française.
    </p>

    <div class="mt-12 grid gap-10 lg:grid-cols-[1fr_320px] items-center">
      <div
        class="relative w-full max-w-xl mx-auto"
        style="perspective: 1200px;"
      >
        <div class="map-tilt">
          <svg viewBox="0 0 600 600" class="w-full h-auto" role="img" aria-label="Carte de France avec datacenters">
            <defs>
              <radialGradient id="fr-fill" cx="50%" cy="50%" r="70%">
                <stop offset="0%" stop-color="#131815" />
                <stop offset="100%" stop-color="#0e1310" />
              </radialGradient>
              <radialGradient id="dc-glow" cx="50%" cy="50%" r="50%">
                <stop offset="0%" stop-color="#c5f04a" stop-opacity="0.8" />
                <stop offset="100%" stop-color="#c5f04a" stop-opacity="0" />
              </radialGradient>
            </defs>
            <polygon
              points={outlinePoints}
              fill="url(#fr-fill)"
              stroke="#c5f04a"
              stroke-opacity="0.35"
              stroke-width="1.5"
            />
            {
              frDcs.map((d) => {
                const p = project(d.lat, d.lon);
                return (
                  <g>
                    <circle cx={p.x} cy={p.y} r="18" fill="url(#dc-glow)" />
                    <circle cx={p.x} cy={p.y} r="4" fill="#c5f04a">
                      <title>{d.name}</title>
                    </circle>
                  </g>
                );
              })
            }
          </svg>
        </div>
      </div>

      <aside class="space-y-4">
        <p class="text-xs uppercase tracking-widest text-ivory-3">
          Datacenters FR connus
        </p>
        <p class="font-display text-6xl text-lime-signature leading-none">
          {frDcs.length}
        </p>
        <p class="text-xs text-ivory-3">
          sur 28 référencés Europe — données PUE / WUE / intensité carbone agrégées.
        </p>
        <ul class="mt-4 space-y-1 text-sm">
          {
            frDcs.map((d) => (
              <li class="text-ivory-2">
                <span class="font-mono text-xs text-ivory-3 mr-2">•</span>
                {d.name}
              </li>
            ))
          }
        </ul>
        <p class="text-xs text-ivory-3 mt-6">
          Source :
          <a class="underline underline-offset-4" href="/docs/sources/CATALOGUE-DATACENTERS/">
            catalogue datacenters
          </a>
          · 28 entries Europe.
        </p>
      </aside>
    </div>
  </div>
</section>

<style>
  .map-tilt {
    transform: rotateX(28deg) rotateZ(-2deg);
    transform-origin: center;
    transition: transform 600ms ease-out;
  }
  .map-tilt:hover {
    transform: rotateX(18deg) rotateZ(0);
  }
  @media (prefers-reduced-motion: reduce) {
    .map-tilt {
      transform: none !important;
    }
  }
</style>
```

---

## Task 4 — Section Entreprises (SVG inline schema)

**Files:**
- Create: `site/src/components/sections/Entreprises.astro`

- [ ] **Step 4.1 — Write `Entreprises.astro`**

2 cards. Card 1 = Mode Équipe self-hosted (disponible aujourd'hui) avec SVG schéma serveur + 4 avatars. Card 2 = Cloud managé (Coming soon) avec CTA mailing list.

```astro
---
import Server from '@lucide/svelte/icons/server';
import Users from '@lucide/svelte/icons/users';
import Cloud from '@lucide/svelte/icons/cloud';
import Mail from '@lucide/svelte/icons/mail';
---

<section id="entreprises" class="border-b border-ivory-4/20">
  <div class="max-w-6xl mx-auto px-6 py-24">
    <p class="text-xs uppercase tracking-widest text-ivory-3">8 — Pour les entreprises</p>
    <h2 class="mt-2 text-4xl md:text-5xl font-display text-ivory">
      Mode Équipe <span class="italic text-lime-signature">aujourd'hui</span>. Cloud
      <span class="italic text-lime-signature">bientôt</span>.
    </h2>
    <p class="mt-6 text-ivory-2 max-w-2xl">
      Deux pistes, un seul produit. La piste self-hosted est disponible dès maintenant
      (cf.
      <a class="underline underline-offset-4" href="/adrs/ADR-0014-dual-track-local-cloud/">
        ADR-0014
      </a>
      ). La piste managée arrivera en v1.3 — sans cloud central avant que la souveraineté
      ne soit garantie.
    </p>

    <div class="mt-12 grid gap-6 md:grid-cols-2">
      <article
        class="rounded-lg border border-lime-signature/40 bg-lime-signature/5 p-8 flex flex-col"
      >
        <div class="flex items-start justify-between">
          <div class="flex items-center gap-3">
            <Server size={28} class="text-lime-signature" />
            <h3 class="font-sans text-xl text-ivory">Mode Équipe self-hosted</h3>
          </div>
          <span
            class="inline-flex items-center rounded-full border border-lime-signature/40 bg-lime-signature/15 text-lime-signature px-2.5 py-0.5 text-xs"
          >
            Disponible
          </span>
        </div>

        <p class="mt-4 text-sm text-ivory-2 leading-relaxed">
          Un binaire serveur léger qui agrège les empreintes de chaque membre de l'équipe.
          100 % local, aucune donnée chez nous. Idéal DSI / RSE.
        </p>

        <div class="my-8 flex items-center justify-center">
          <svg viewBox="0 0 320 160" class="w-full max-w-[280px] h-auto" aria-hidden="true">
            <defs>
              <radialGradient id="server-glow" cx="50%" cy="50%" r="50%">
                <stop offset="0%" stop-color="#c5f04a" stop-opacity="0.35" />
                <stop offset="100%" stop-color="#c5f04a" stop-opacity="0" />
              </radialGradient>
            </defs>
            <circle cx="160" cy="80" r="56" fill="url(#server-glow)" />
            <rect
              x="135"
              y="55"
              width="50"
              height="50"
              rx="6"
              fill="#0e1310"
              stroke="#c5f04a"
              stroke-width="1.5"
            />
            <circle cx="146" cy="68" r="2" fill="#c5f04a" />
            <circle cx="146" cy="80" r="2" fill="#c5f04a" />
            <circle cx="146" cy="92" r="2" fill="#c5f04a" />
            <line
              x1="160"
              y1="55"
              x2="160"
              y2="20"
              stroke="#c5f04a"
              stroke-opacity="0.35"
              stroke-dasharray="3 3"
            />
            <line
              x1="160"
              y1="105"
              x2="160"
              y2="140"
              stroke="#c5f04a"
              stroke-opacity="0.35"
              stroke-dasharray="3 3"
            />
            <line
              x1="135"
              y1="80"
              x2="80"
              y2="80"
              stroke="#c5f04a"
              stroke-opacity="0.35"
              stroke-dasharray="3 3"
            />
            <line
              x1="185"
              y1="80"
              x2="240"
              y2="80"
              stroke="#c5f04a"
              stroke-opacity="0.35"
              stroke-dasharray="3 3"
            />
            <g>
              <circle cx="160" cy="14" r="11" fill="#131815" stroke="#f0ece3" stroke-width="1" />
              <circle cx="160" cy="11" r="3.2" fill="#f0ece3" />
              <path d="M153 18 Q160 23 167 18 L167 22 L153 22 Z" fill="#f0ece3" />
            </g>
            <g>
              <circle cx="160" cy="146" r="11" fill="#131815" stroke="#f0ece3" stroke-width="1" />
              <circle cx="160" cy="143" r="3.2" fill="#f0ece3" />
              <path d="M153 150 Q160 155 167 150 L167 154 L153 154 Z" fill="#f0ece3" />
            </g>
            <g>
              <circle cx="74" cy="80" r="11" fill="#131815" stroke="#f0ece3" stroke-width="1" />
              <circle cx="74" cy="77" r="3.2" fill="#f0ece3" />
              <path d="M67 84 Q74 89 81 84 L81 88 L67 88 Z" fill="#f0ece3" />
            </g>
            <g>
              <circle cx="246" cy="80" r="11" fill="#131815" stroke="#f0ece3" stroke-width="1" />
              <circle cx="246" cy="77" r="3.2" fill="#f0ece3" />
              <path d="M239 84 Q246 89 253 84 L253 88 L239 88 Z" fill="#f0ece3" />
            </g>
          </svg>
        </div>

        <ul class="mt-2 space-y-2 text-sm text-ivory-2">
          <li class="flex items-start gap-2">
            <span class="text-lime-signature mt-1">→</span> Binaire serveur léger (~10 MB)
          </li>
          <li class="flex items-start gap-2">
            <span class="text-lime-signature mt-1">→</span> Pairing chiffré via extension
            navigateur
          </li>
          <li class="flex items-start gap-2">
            <span class="text-lime-signature mt-1">→</span> Dashboard agrégé scope 3 IA,
            export CSRD
          </li>
        </ul>

        <a
          href="/docs/operations/team-aggregator/"
          class="mt-6 inline-flex items-center justify-center gap-2 rounded-md bg-lime-signature text-ink px-4 py-2 text-sm font-sans font-medium hover:bg-lime-deep transition-colors"
        >
          <Users size={16} />
          Voir le guide Mode Équipe
        </a>
      </article>

      <article
        class="rounded-lg border border-ivory-4/40 bg-ink-2 p-8 flex flex-col"
      >
        <div class="flex items-start justify-between">
          <div class="flex items-center gap-3">
            <Cloud size={28} class="text-amber" />
            <h3 class="font-sans text-xl text-ivory">Cloud managé</h3>
          </div>
          <span
            class="inline-flex items-center rounded-full border border-amber/40 bg-amber/10 text-amber px-2.5 py-0.5 text-xs"
          >
            v1.3 — bientôt
          </span>
        </div>

        <p class="mt-4 text-sm text-ivory-2 leading-relaxed">
          Pour les organisations qui ne peuvent pas héberger, une offre Cloud arrivera
          avec souveraineté garantie, chiffrement bout-en-bout et opt-in explicite RGPD.
          Pas de cloud Sobr.ia avant que ces conditions soient remplies.
        </p>

        <div class="flex-1"></div>

        <ul class="mt-6 space-y-2 text-sm text-ivory-2">
          <li class="flex items-start gap-2">
            <span class="text-amber mt-1">→</span> Datacenter EU, hébergeur certifié
            (HDS-SecNumCloud à étudier)
          </li>
          <li class="flex items-start gap-2">
            <span class="text-amber mt-1">→</span> Onboarding équipes, SSO, RBAC
          </li>
          <li class="flex items-start gap-2">
            <span class="text-amber mt-1">→</span> Tarif beta — early access mailing list
          </li>
        </ul>

        <a
          href="/cloud/"
          class="mt-6 inline-flex items-center justify-center gap-2 rounded-md border border-ivory-4 text-ivory hover:border-lime-signature hover:text-lime-signature px-4 py-2 text-sm font-sans font-medium transition-colors"
        >
          <Mail size={16} />
          Être prévenu·e
        </a>
      </article>
    </div>
  </div>
</section>
```

---

## Task 5 — Wire to index.astro + verify + commit

**Files:**
- Modify: `site/src/pages/index.astro`

- [ ] **Step 5.1 — Edit `index.astro`** : remplace les 5 placeholders restants par les 4 vraies sections (4 Download reste placeholder pour C33.5).

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '$components/sections/Hero.astro';
import PourQui from '$components/sections/PourQui.astro';
import CommentCaMarche from '$components/sections/CommentCaMarche.astro';
import Methodologies from '$components/sections/Methodologies.astro';
import VendorDisclosure from '$components/sections/VendorDisclosure.astro';
import TerritoireFR from '$components/sections/TerritoireFR.astro';
import Entreprises from '$components/sections/Entreprises.astro';
---

<BaseLayout
  title="Sobr.ia — l'empreinte de vos prompts IA, mesurée"
  description="Application native multi-plateforme qui mesure l'empreinte carbone, eau, énergie de chaque prompt LLM. Frugal, open source, candidat défi data.gouv.fr."
>
  <Hero />
  <PourQui />
  <CommentCaMarche />

  <section id="download" class="min-h-[60vh] border-b border-ivory-4/20">
    <div class="max-w-6xl mx-auto px-6 py-24">
      <p class="text-xs uppercase tracking-widest text-ivory-3">
        4 — Télécharger · Auto-détection OS (C33.5)
      </p>
      <h2 class="mt-2 text-4xl md:text-5xl font-display text-ivory">Bientôt</h2>
      <p class="mt-6 text-ivory-3 max-w-2xl">
        Section construite en C33.5 avec auto-détection OS + workflows release CI.
      </p>
    </div>
  </section>

  <Methodologies />
  <VendorDisclosure />
  <TerritoireFR />
  <Entreprises />
</BaseLayout>
```

- [ ] **Step 5.2 — Format + lint + check + build**

```bash
cd site && npm run format && npm run lint && npm run check && npm run build
```

Expected: all pass. Build report should still respect the bundle budgets:
- Initial JS ≤ 100 KB gzip (essentiellement inchangé : MonteCarloViz est un client:visible séparé).
- 3D bundle Three.js shared with HeroGlobe → MonteCarloViz reuses the same lazy chunk.

- [ ] **Step 5.3 — Update CHANGELOG + commit**

Add to `[Unreleased]` :

```markdown
### Added — C33.3 4 sections 3D animées (2026-05-17)

- MonteCarloViz.svelte : Three.js InstancedMesh 10 000 sphères, distribution
  gaussienne autour d'axe X avec convergence animée au scroll-entry, toggle
  AFNOR/Sobr.ia vs EcoLogits (lerp positions cibles), P5/P50/P95 affichés
  en stats live + bandes plan ambre P5/P95 dans la scène.
- VendorDisclosure.astro : table 5 vendors (Mistral×ADEME / Google Gemini /
  Meta Llama 3.x / Anthropic / OpenAI), chiffres clés (1,14 g / 0,03 g /
  11 390 t), badges ✓/~/✕ par disclosure, révélation séquentielle IO.
- TerritoireFR.astro : carte FR SVG outline stylisé + datacenters FR filtrés
  depuis datacenters.json, projection lat/lon → viewBox, CSS perspective +
  rotateX pour effet 2.5D, hover → unfold partiel. prefers-reduced-motion ok.
- Entreprises.astro : 2 cards (Mode Équipe self-hosted dispo / Cloud managé
  v1.3 bientôt), SVG inline schéma serveur + 4 avatars employés, CTAs
  /docs/operations/team-aggregator/ et /cloud/.
- index.astro : sections 5/6/7/8 wirées, seul download (4) reste placeholder
  pour C33.5.
```

```bash
git add site/ docs/superpowers/plans/2026-05-17-c33.3-animated-sections.md
git commit -m "feat(site): C33.3 sections 3D animées (Monte-Carlo, Vendors, Territoire FR, Entreprises)"
```

(Message complet en bash heredoc lors du commit.)

---

## DoD — C33.3

- [ ] Section 5 Méthodologies : Monte-Carlo 10000 instances visibles, toggle method fonctionne, stats live, fallback prefers-reduced-motion.
- [ ] Section 6 Vendors : 5 lignes apparaissent en stagger au scroll, chiffres et badges lisibles.
- [ ] Section 7 Territoire FR : carte SVG tiltée avec datacenters dots lime, tooltip native title sur hover.
- [ ] Section 8 Entreprises : 2 cards, SVG serveur+avatars rendu, CTAs vers docs Mode Équipe et /cloud/.
- [ ] `npm run lint`, `npm run check`, `npm run build` passent.
- [ ] Bundle initial ≤ 100 KB gzip.
- [ ] Bundle 3D lazy ≤ 300 KB gzip.

---

## Self-review

- Spec coverage: Monte-Carlo ✓, Vendors ✓, IRIS map (SVG tilté décision arbitrée) ✓, Entreprises ✓.
- Placeholders: aucun code TBD ; seule section restante = #download (C33.5).
- Assumptions :
  - Le `$effect.root` + `$effect` à l'intérieur de `onMount` pour réagir au changement de `method` peut nécessiter ajustement (Svelte 5 — alternative : poser un watcher avec `$effect` au top-level du `<script>`).
  - Le rendering CSS `transform: rotateX(28deg)` sur le SVG doit fonctionner sur tous les navigateurs cibles ; testé Chrome/Firefox/Safari récents.
