# Icônes Sobr.ia

Tauri attend ici les icônes packagées :

- `icon.png` — 1024×1024 PNG (utilisé par défaut).
- `icon.icns` (macOS), `icon.ico` (Windows) — générés via `tauri icon`.

Placeholder : remplacer par les assets finaux issus du design Claude
(URL `https://api.anthropic.com/v1/design/h/pzqXRiIjWHO1dkAt2b91cw`).

Commande de génération :

```bash
cd crates/sobria-app
npx tauri icon path/to/source-1024.png
```
