// Déclarations globales TypeScript de l'app SvelteKit.

declare global {
  /** Version applicative injectée par Vite `define` (cf. vite.config.ts, C36). */
  const __APP_VERSION__: string;
}

export {};
